# ============================================================
# 安全方案 - bearerAuth / apiKeyAuth
# ============================================================

bearerAuth:
  type: http
  scheme: bearer
  bearerFormat: JWT

apiKeyAuth:
  type: apiKey
  in: header
  name: X-Api-Key
  description: 第三方平台 API Key
